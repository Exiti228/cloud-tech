spring:
  application:
    name: kinopoisk
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
minio:
  url: http://localhost:9000
  login: minioadmin
  password: minioadmin
